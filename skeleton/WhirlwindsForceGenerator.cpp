#include "WhirlwindsForceGenerator.h"

WhirlwindsForceGenerator::WhirlwindsForceGenerator(double k1, double K, Vector3 origin, double duration)
{
}

void WhirlwindsForceGenerator::updateForce(Particle* particle)
{
}
